global:
  scrape_interval: 1s
  evaluation_interval: 1s
rule_files:
  - /etc/prometheus/rules/backtest-worker-alerts.yml
scrape_configs:
  - job_name: backtest-worker-validation
    static_configs:
      - targets: [pushgateway:9091]

