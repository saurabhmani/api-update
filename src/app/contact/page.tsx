import SiteShell from '@/components/corporate/SiteShell'; import ContactForm from '@/components/corporate/ContactForm'; import { PageHero, Section } from '@/components/corporate/PageParts'; import { brand } from '@/content/corporate'; export const metadata={title:'Contact'}; export default function Contact(){return <SiteShell><PageHero eyebrow="Contact" title="Let’s make the next move clearer." body="Tell us what you’re trying to change, improve, or build. We’ll start with a considered conversation."/><Section><div className="q-detail-grid"><div><p className="q-eyebrow">Start a conversation</p><h2>Useful context is a great first step.</h2><p className="q-body">This form is configured for safe validation only. Connect it to your preferred CRM or email provider before launch.</p><p style={{marginTop:24}}><a className="q-link" href={`mailto:${brand.contactEmail}`}>{brand.contactEmail}</a></p></div><ContactForm/></div></Section></SiteShell>}
