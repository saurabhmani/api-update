import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import SiteShell from '@/components/corporate/SiteShell'; import { PageHero, CTA, Section } from '@/components/corporate/PageParts'; import { industries } from '@/content/corporate'; export const metadata={title:'Industries'}; export default function Industries(){return <SiteShell><PageHero eyebrow="Industries" title="Built for organizations working through change." body="Industry pages are deliberately limited to areas where draft content is ready for validation and replacement."/><Section><div className="q-card-grid">{industries.map(x=><article className="q-card" key={x.slug}><div><p className="q-eyebrow">Draft industry content</p><h3>{x.name}</h3><p>{x.headline}</p></div><Link className="q-link" href={`/industries/${x.slug}`}>Explore industry <ArrowUpRight size={16}/></Link></article>)}</div></Section><CTA/></SiteShell>}
