import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import SiteShell from '@/components/corporate/SiteShell'; import { PageHero, CTA, Section } from '@/components/corporate/PageParts'; import { industries, services, technologies } from '@/content/corporate'; export const metadata={title:'Industries',description:'Technology delivery perspectives for organisations operating through change.',alternates:{canonical:'/industries'}}; export default function Industries(){return <SiteShell><PageHero eyebrow="Industries" title="Built for organizations working through change." body="Editable industry perspectives shaped around common operating challenges—not unsupported claims of sector expertise." crumbs={[{label:'Industries'}]}/><Section><div className="q-card-grid">{industries.map(x=><article className="q-card" key={x.slug}><div><p className="q-eyebrow">Industry perspective</p><h3>{x.name}</h3><p>{x.headline}</p></div><Link className="q-link" href={`/industries/${x.slug}`}>Explore industry <ArrowUpRight size={16}/></Link></article>)}</div></Section><Section className="alt"><div className="q-detail-grid"><div><p className="q-eyebrow">Technology capabilities</p><h2>Adaptable to the work at hand.</h2></div><div><div className="q-tags">{technologies.map(t=><Link href={`/technologies/${t.slug}`} className="q-tag" key={t.slug}>{t.name}</Link>)}</div><h2>Relevant services</h2><div className="q-tags">{services.map(s=><Link href={`/services/${s.slug}`} className="q-tag" key={s.slug}>{s.name}</Link>)}</div></div></div></Section><CTA/></SiteShell>}
