import { notFound } from 'next/navigation'; import SiteShell from '@/components/corporate/SiteShell'; import { PageHero, CTA, Section } from '@/components/corporate/PageParts'; import { industries, services } from '@/content/corporate'; export function generateStaticParams(){return industries.map(x=>({slug:x.slug}))} export default async function Industry({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const i=industries.find(x=>x.slug===slug);if(!i)return notFound();return <SiteShell><PageHero eyebrow="Industry · Draft" title={i.headline} body="This page uses replaceable draft content until industry experience is confirmed through approved source material."/><Section><div className="q-detail-grid"><div><p className="q-eyebrow">Current challenges</p><h2>Where attention is needed.</h2></div><div><ul>{i.challenges.map(x=><li key={x}>{x}</li>)}</ul><h2>Relevant capabilities</h2><div className="q-tags">{services.map(s=><span className="q-tag" key={s.slug}>{s.name}</span>)}</div><h2>Case studies</h2><p className="q-body">No verified industry-specific stories have been published yet.</p></div></div></Section><CTA/></SiteShell>}
