import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import SiteShell from '@/components/corporate/SiteShell'; import { PageHero, CTA, Section } from '@/components/corporate/PageParts'; import { services } from '@/content/corporate'; export const metadata={title:'Services'}; export default function Services(){return <SiteShell><PageHero eyebrow="Services" title="The capability to turn plans into progress." body="Strategy, design, engineering, data, and automation brought together around the work that matters."/><Section><div className="q-service-list">{services.map((s,i)=><Link className="q-service" key={s.slug} href={`/services/${s.slug}`}><span>0{i+1}</span><div><h3>{s.name}</h3><p>{s.short}</p></div><ArrowUpRight/></Link>)}</div></Section><CTA/></SiteShell>}
