import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import SiteShell from '@/components/corporate/SiteShell'; import { PageHero, CTA, Section } from '@/components/corporate/PageParts'; import { services, industries, technologies } from '@/content/corporate'; export const metadata={title:'Services',description:'Technology strategy, AI, data, cloud, cybersecurity, engineering, automation, and managed services.',alternates:{canonical:'/services'}}; export default function Services(){return <SiteShell><PageHero eyebrow="Services" title="The capability to turn plans into progress." body="Strategy, design, engineering, data, and automation brought together around the work that matters." crumbs={[{label:'Services'}]}/><Section><div className="q-service-list">{services.map((s,i)=><Link className="q-service" key={s.slug} href={`/services/${s.slug}`}><span>{String(i+1).padStart(2,'0')}</span><div><h3>{s.name}</h3><p>{s.short}</p></div><ArrowUpRight/></Link>)}</div></Section><Section className="alt"><div className="q-detail-grid"><div><p className="q-eyebrow">Delivery methodology</p><h2>Discover. Design. Engineer. Evolve.</h2></div><div><p className="q-body">We start with operating context, turn it into a focused service or product plan, build in useful increments, and improve alongside the teams responsible for the work.</p><h2>Related industries</h2><div className="q-tags">{industries.map(i=><Link className="q-tag" key={i.slug} href={`/industries/${i.slug}`}>{i.name}</Link>)}</div><h2>Technology stack</h2><div className="q-tags">{technologies.map(t=><Link className="q-tag" key={t.slug} href={`/technologies/${t.slug}`}>{t.name}</Link>)}</div></div></div></Section><CTA/></SiteShell>}
