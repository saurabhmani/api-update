// Canonical AppShell lives at src/components/layout/AppShell.tsx.
// This file is a thin re-export so any historical import path keeps
// working. Do not add UI here — there must be a single source of truth.
export { default } from '@/components/layout/AppShell';
