import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import SiteShell from '@/components/corporate/SiteShell'; import { CTA, PageHero, Section } from '@/components/corporate/PageParts'; import { technologies } from '@/content/corporate'; export const metadata={title:'Technologies',description:'The technology ecosystem Quantorus uses to deliver resilient, useful digital services.',alternates:{canonical:'/technologies'}}; export default function Technologies(){return <SiteShell><PageHero eyebrow="Technologies" title="Foundations that make the next change possible." body="A clear view of the technology capabilities Quantorus uses to connect strategy with dependable delivery." crumbs={[{label:'Technologies'}]}/><Section><div className="q-card-grid">{technologies.map(x=><article className="q-card" key={x.slug}><div><p className="q-eyebrow">{x.category}</p><h3>{x.name}</h3><p>{x.description}</p></div><Link className="q-link" href={`/technologies/${x.slug}`}>Explore capability <ArrowUpRight size={16}/></Link></article>)}</div></Section><Section className="alt"><div className="q-detail-grid"><div><p className="q-eyebrow">Technology ecosystem</p><h2>Capabilities, not unverified badges.</h2></div><p className="q-body">Partnerships, certifications, and formal platform relationships will be listed here only when verified and approved. This editable section describes the technology ecosystem without making unsupported claims.</p></div></Section><CTA/></SiteShell>}
