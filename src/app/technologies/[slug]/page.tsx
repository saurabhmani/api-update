import { notFound } from 'next/navigation'; import SiteShell from '@/components/corporate/SiteShell'; import { CTA, PageHero, Section } from '@/components/corporate/PageParts'; import { technologies } from '@/content/corporate'; export function generateStaticParams(){return technologies.map(x=>({slug:x.slug}))} export default async function Technology({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const t=technologies.find(x=>x.slug===slug);if(!t)return notFound();return <SiteShell><PageHero eyebrow="Technology capability" title={t.name} body={t.description}/><Section><div className="q-detail-grid"><div><p className="q-eyebrow">Practical application</p><h2>Technology in service of progress.</h2></div><p className="q-body">This capability page is designed as a focused starting point. Quantorus applies technology choices in response to a clear operating context, delivery need, and path for sustainable change.</p></div></Section><CTA/></SiteShell>}
