import { TrustLayerClient } from './TrustLayerClient';

export const metadata = {
  title: 'Trust Layer — Quantorus365',
  description: 'Institutional trust dashboard with signals, regime scanner, and performance',
};

export default function TrustPage() {
  return <TrustLayerClient />;
}
