import type { MetadataRoute } from 'next'; import { services, industries, caseStudies } from '@/content/corporate'; export default function sitemap():MetadataRoute.Sitemap{const root='https://quantorus.example';return ['','/services','/industries','/resources','/about','/careers','/contact','/privacy',...services.map(x=>`/services/${x.slug}`),...industries.map(x=>`/industries/${x.slug}`),...caseStudies.map(x=>`/resources/case-studies/${x.slug}`)].map(url=>({url:root+url,lastModified:new Date()}))}
