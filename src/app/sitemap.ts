import type { MetadataRoute } from 'next'; import { services, industries, caseStudies, technologies, insights } from '@/content/corporate'; export default function sitemap():MetadataRoute.Sitemap{const root='https://quantorus.example';return ['','/services','/industries','/technologies','/resources','/case-studies','/insights','/about','/careers','/contact','/privacy','/terms',...services.map(x=>`/services/${x.slug}`),...industries.map(x=>`/industries/${x.slug}`),...technologies.map(x=>`/technologies/${x.slug}`),...caseStudies.map(x=>`/resources/case-studies/${x.slug}`),...insights.map(x=>`/insights/${x.slug}`)].map(url=>({url:root+url,lastModified:new Date()}))}
