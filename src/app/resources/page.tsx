import SiteShell from '@/components/corporate/SiteShell'; import ResourceList from '@/components/corporate/ResourceList'; import { PageHero, Section, CTA } from '@/components/corporate/PageParts'; export const metadata={title:'Resources'}; export default function Resources(){return <SiteShell><PageHero eyebrow="Resources" title="Work, examined with care." body="Our case-study framework is ready for approved project stories. Current entries are transparently marked as placeholders because the referenced public profile could not be verified."/><Section><p className="q-notice">Development note: no client details, results, or metrics have been fabricated.</p><ResourceList/></Section><CTA/></SiteShell>}
