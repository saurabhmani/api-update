import { notFound } from 'next/navigation'; import type { Metadata } from 'next'; import Link from 'next/link'; import SiteShell from '@/components/corporate/SiteShell'; import { PageHero, CTA, Section } from '@/components/corporate/PageParts'; import { caseStudies } from '@/content/corporate';
export function generateStaticParams(){return caseStudies.map(x=>({slug:x.slug}));} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const c=caseStudies.find(x=>x.slug===slug);return c?{title:c.title,description:c.challenge,alternates:{canonical:`/resources/case-studies/${c.slug}`}}:{};} export default async function CaseStudy({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseStudies.find(x=>x.slug===slug);if(!c)return notFound();return <SiteShell><PageHero eyebrow={`Case study · ${c.verificationStatus}`} title={c.title} body="An anonymized, replacement-ready case-study structure." crumbs={[{label:'Case studies',href:'/resources'},{label:c.title}]}/><Section><div className="q-detail-grid"><aside><p className="q-eyebrow">Engagement</p><div className="q-tags"><span className="q-tag">{c.industry}</span><span className="q-tag">{c.service}</span></div></aside><article className="q-prose"><h2>Project overview</h2><p>Client description withheld pending approval. This entry is intentionally structured to be replaced with verified public content.</p><h2>Business challenge</h2><p>{c.challenge}</p><h2>Solution</h2><p>{c.solution}</p><h2>Technologies and engagement process</h2><p>Technology choices and engagement detail will be published only when approved.</p><h2>Business outcome</h2><p>{c.result}</p><h2>Related services and industries</h2><p>{c.service} · {c.industry}</p><p className="q-notice">This content is a {c.verificationStatus} placeholder. No client names, testimonials, metrics, or revenue impact have been fabricated.</p><p><Link className="q-link" href="/resources">Back to case studies</Link></p></article></div></Section><CTA/></SiteShell>;}
