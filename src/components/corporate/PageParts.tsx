import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { Reveal } from './Reveal';
export function Breadcrumbs({ items }: { items: { label: string; href?: string }[] }) { return <nav className="q-breadcrumbs" aria-label="Breadcrumb"><ol><li><Link href="/">Home</Link></li>{items.map(item => <li key={item.label}>{item.href ? <Link href={item.href}>{item.label}</Link> : <span aria-current="page">{item.label}</span>}</li>)}</ol></nav>; }
export function PageHero({ eyebrow, title, body, crumbs }: { eyebrow: string; title: string; body: string; crumbs?: { label: string; href?: string }[] }) { return <section className="q-page-hero"><div className="q-container">{crumbs && <Breadcrumbs items={crumbs}/>}<p className="q-eyebrow">{eyebrow}</p><h1>{title}</h1><p>{body}</p></div></section>; }
export function StructuredData({ data }: { data: object }) { return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }} />; }
export function CTA() { return <section className="q-section alt"><div className="q-container q-scheme"><p className="q-eyebrow">Start here</p><h2>Bring the hard problem into focus.</h2><p>We’ll start with the context, constraints, and progress that matters to your team.</p><Link className="q-talk" href="/contact">Start a conversation <ArrowUpRight size={16}/></Link></div></section>; }
export function Section({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <section className={`q-section ${className}`}><div className="q-container"><Reveal>{children}</Reveal></div></section>; }
