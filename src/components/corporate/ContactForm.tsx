'use client';
import { useState } from 'react';
export default function ContactForm() { const [state, setState] = useState<'idle'|'success'|'error'>('idle'); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const form = e.currentTarget; if (!form.checkValidity()) { form.reportValidity(); return; } try { const r = await fetch('/api/contact', { method: 'POST', body: new FormData(form) }); setState(r.ok ? 'success' : 'error'); } catch { setState('error'); } } if(state==='success') return <div className="q-success"><b>Thank you — your message is ready for review.</b><p>This demonstration endpoint does not deliver email yet. Connect a CRM or email provider in <code>/api/contact</code> before launch.</p></div>; return <form className="q-form" onSubmit={submit}><label>Name<input required name="name" autoComplete="name" /></label><label>Work email<input required type="email" name="email" autoComplete="email" /></label><label>Company<input name="company" autoComplete="organization" /></label><label>Service interest<select name="service"><option>Choose a service</option><option>AI, Data & Intelligent Automation</option><option>Digital Product Engineering</option><option>Cloud & Platform Modernization</option><option>Technology Strategy & Consulting</option></select></label><label>Budget range<select name="budget"><option>Choose a range</option><option>Under $25k</option><option>$25k–$75k</option><option>$75k+</option></select></label><label className="full">Project summary<textarea required name="summary" rows={5} /></label><label className="consent full"><input required type="checkbox" name="consent" /> I agree that Quantorus may use this information to respond to my request.</label>{state==='error' && <p role="alert">Something went wrong. Please try again later.</p>}<button className="q-talk" type="submit">Start the conversation</button></form>; }
