# ADR-021: Backtest canary and rollback gates

- Status: Proposed

Any duplicate claim, owner overlap, parity mismatch, unexpected dead job/recovery, or cancellation failure stops the canary. Rollback is dry-run-first, requires disabled ownership, preserves attempts, and never deletes queue data.
