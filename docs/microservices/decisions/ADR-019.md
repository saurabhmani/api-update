# ADR-019: Backtest staging deployment model

- Status: Proposed

Use one staging-only PM2 process consistent with the VPS operating model. It starts disabled, binds loopback-only, uses concurrency one, and is never routed through Nginx.
