# ADR-020: Backtest ownership configuration consistency

- Status: Proposed

Distribute one deployment-managed environment value to monolith and worker. Every handoff passes through disabled and restarts both processes. Staging remains blocked if the preflight cannot verify consistent observable configuration.
