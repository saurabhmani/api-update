# ADR-018: Backtest lease timing and recovery

- Status: Proposed

Lease duration must remain at least three heartbeat intervals. Integration tests use controlled clocks rather than long sleeps. Expired retryable work retains attempts and returns to queued; exhausted work becomes dead; expired cancellation requests become cancelled.
